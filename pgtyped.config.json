{
    "transforms": [
        {
            "mode": "sql",
            "include": "server/queries/**/*.sql",
            "emitTemplate": "{{dir}}/{{name}}.queries.ts"
        }
    ],
    "srcDir": "./src/"
}